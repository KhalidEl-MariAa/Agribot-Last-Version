import 'package:flutter/material.dart';

class Mater extends StatelessWidget {
  const Mater({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.grey[400],
    );
  }
}
